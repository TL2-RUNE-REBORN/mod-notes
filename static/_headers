# Cloudflare Pages custom headers (published verbatim from static/).
#
# /tools/packer/ 需要跨源隔离(crossOriginIsolated)才能用 SharedArrayBuffer /
# wasm threads —— 多线程打包的硬前提。只限打包器路径,避免 COEP 波及全站
# 其他页面的跨源子资源。
/tools/packer/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp

# 英文版打包页(/en/tools/packer/)复用同一份 worker/wasm/base,只换文案。
# 不给它同样的头,英文用户就永远拿不到 crossOriginIsolated,静默退回单线程。
/en/tools/packer/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
